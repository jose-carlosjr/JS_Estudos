let dog = {
    nome: 'Shark',
    patas: 4,
    raca: 'Poodle',
    latir: function() {
        return 'Au Au'
    }
}

console.log(`${dog.nome} tem ${dog.patas} patas, é da raça: ${dog.raca} e faz ${dog.latir()}`)


// IMPORTANTE!

// [] array
// {} objeto